import { IIconButton } from './IconButton';

const base: IIconButton = {
  category: 'lock',
  state: 'primary',
  disabled: false,
  iconSize: 'xs',
  inverted: false,
  className: '',
};

export const mockIconButtonProps = {
  base,
};
